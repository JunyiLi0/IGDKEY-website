import React from "react";

export function Room(props) {
    return (
        <group {...props}>
            {/* Placeholder for removed 3D room model */}
        </group>
    );
}

export default Room; 